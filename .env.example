# 建议设置一个随机长字符串；设置后访问网页和订阅都必须带 ?token=...
ACCESS_TOKEN=change-me-to-a-long-random-token

# 你的 subconverter 地址，不要带 /sub
SUBAPI=https://subconverter.example.com

# 可选：默认远程 .ini URL
SUBCONFIG=

# 可选：不填则继续读取本仓库 GitHub Raw 默认地址
BEST_IP_CSV=
FIXED_ADDRESSES_URL=
